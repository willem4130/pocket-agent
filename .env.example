# Anthropic API Key (required)
ANTHROPIC_API_KEY=your-api-key-here

# Telegram Bot Token (optional)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Comma-separated list of allowed Telegram user IDs (optional)
TELEGRAM_ALLOWED_USERS=

# Browser Automation (built-in 3-tier system)
# For CDP tier (authenticated sessions), start Chrome with:
# /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222
CDP_URL=http://localhost:9222

# Desktop Automation via Computer Use (optional)
# Runs in Docker container for safety
COMPUTER_USE_ENABLED=false
